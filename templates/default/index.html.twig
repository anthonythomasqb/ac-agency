{% extends "base.html.twig"%}

 {% block body %}
     <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
         <a class="navbar-brand" href="#"> Movies list </a>
         {% if app.user %}
             <a class="navbar-brand" href="{{ path("logout") }}">Log out</a>
         {% else %}
             <a class="navbar-brand" href="{{ path("login") }}">Log in</a>
         {% endif %}
     </nav>

     <div id="Movies" class="container"></div>
 {% endblock %}